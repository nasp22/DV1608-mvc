<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="{{ asset('img/favicon.jpg') }}">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <header class="site-header" style="background-image: url({{ asset('img/header.jpg') }})">
        </header>
        <nav class="navbar">
            <ul>
                <li><a href="{{ path('home') }}">Home</a></li>
                <li><a href="{{ path('about') }}">About</a></li>
                <li><a href="{{ path('report') }}">Redovisa</a></li>
                <li><a href="{{ path('lucky') }}">Lucky animal</a></li>
                <li><a href="{{ path('quote') }}"> Dagens citat</a></li>
                <li><a href="{{ path('card_start') }}"> Cards (kmom02)</a></li>
                <li><a href="{{ path('21_init') }}"> Card Game - 21 (kmom03) </a></li>
                <li><a href="{{ path('library') }}"> Library (kmom05) </a></li>
                <li><a href="{{ path('metrics') }}"> Metrics (kmom06)</a></li>
                <li><a href="{{ path('api') }}"> API JSON</a></li>
            </ul>
        </nav>
    <main>
        {% block body %}{% endblock %}
    </main>
    <footer>
        {% block footer %}
            &copy; Nadja Spångberg
        {% endblock %}
    </footer>
    </body>
</html>
